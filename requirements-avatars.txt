# Dependencias SOLO para la generación de avatares (cron web-stats-generate-avatars).
# NO se instalan en el servicio web (se mantiene ligero). numpy/opencv-headless/Pillow ya
# están en requirements.txt.
insightface==0.7.3
onnxruntime==1.18.1
